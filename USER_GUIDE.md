# คู่มือการใช้งาน (User Guide)

## 1. การติดตั้ง (Installation)

### สิ่งที่ต้องมี (Prerequisites)
1. **Python 3.8+**: ดาวน์โหลดได้ที่ [python.org](https://www.python.org/)
2. **Poppler**: จำเป็นสำหรับการแปลงไฟล์ PDF
   - ดาวน์โหลด: [Poppler for Windows](https://github.com/oschwartz10612/poppler-windows/releases/)
   - วิธีติดตั้ง: แตกไฟล์และนำ path ของโฟลเดอร์ `bin` (เช่น `C:\Program Files\poppler-xx\bin`) ไปเพิ่มใน System Environment Variables (PATH)

### ขั้นตอนการติดตั้ง (Setup Steps)
1. **Clone Project** หรือดาวน์โหลดไฟล์โปรเจคมาที่เครื่อง
2. เปิด Terminal (Command Prompt หรือ PowerShell) ในโฟลเดอร์โปรเจค
3. ติดตั้ง Libraries ที่จำเป็น:
   ```bash
   pip install -r requirements.txt
   ```

## 2. การใช้งาน (Usage)

### การรันโปรแกรม
พิมพ์คำสั่งต่อไปนี้ใน Terminal:
```bash
streamlit run app.py
```
โปรแกรมจะเปิดขึ้นมาใน Web Browser โดยอัตโนมัติ

### การใช้งานหน้าเว็บ
1. **ตั้งค่า API Key**:
   - ที่แถบด้านซ้าย ใส่ **Gemini API Key** ของคุณ (ขอได้ฟรีที่ [Google AI Studio](https://aistudio.google.com/))
   - *หมายเหตุ: หากคุณ Deploy บน Hugging Face สามารถตั้งค่าใน Secrets ได้เลย*

2. **อัปโหลดไฟล์**:
   - เลือก Tab **"📂 File Upload"** เพื่ออัปโหลดไฟล์ PDF จากเครื่อง
   - หรือเลือก Tab **"☁️ Google Drive"** เพื่อใส่ลิงก์ Google Drive Folder

3. **เริ่มประมวลผล**:
   - กดปุ่ม **"Start Processing Files"**
   - รอระบบทำงาน (ความเร็วขึ้นอยู่กับจำนวนหน้าและ API Limit)

4. **ดาวน์โหลดผลลัพธ์**:
   - เมื่อเสร็จสิ้น จะมีปุ่มให้ดาวน์โหลดไฟล์ CSV ทั้งหมดในรูปแบบ ZIP
   - ไฟล์ CSV ที่ได้จะประกอบด้วย:
     - `Train_summary.csv`
     - `Test_doc_info.csv`
     - `Test_nacc_detail.csv`
     - `Test_submitter_info.csv`
     - `token_usage_report.csv` (รายงานการใช้ Token และจำนวนหน้า)
     - และไฟล์รายละเอียดอื่นๆ
